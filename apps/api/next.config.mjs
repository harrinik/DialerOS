// next.config.mjs — kept in sync with next.config.ts
// Both files MUST have identical output settings.
// Next.js 14 may load either one depending on the environment.
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
};
export default nextConfig;
